import React from 'react';

const Items = ({ clickedItem }) => {
  console.log('hahaha', clickedItem);
  return (
    <div>
      {/* <p>{clickedItem.name}</p>
      <img src={clickedItem.image_url} /> */}
    </div>
  );
};

export default Items;
